<p style="color: green"><%= notice %></p>

<h1>Games</h1>

<div id="games">
<%= link_to "New game", new_game_path %>

  <% @games.each do |game| %>
    <%= render game %>
    <p>
      <%= link_to "Show this game", game %>
    </p>
  <% end %>
</div>

